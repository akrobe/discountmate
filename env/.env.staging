# env/.env.staging
# Non-secrets only. Secrets should come from Jenkins credentials or Docker secrets.
APP_PORT=8081
# Add app-specific env below if needed, e.g.:
# MODEL_VARIANT=baseline

# optional but allowed; Jenkins sets IMAGE during CI, local runs will use the fallback above
# IMAGE=ghcr.io/akrobe/discountmate:<some-tag>