groups:
- name: discountmate.rules
  rules:
  - alert: DiscountmateErrors
    expr: increase(dm_errors_total[1m]) > 0
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "DiscountMate errors detected"
      description: "simulate_error endpoint triggered within the last minute."
