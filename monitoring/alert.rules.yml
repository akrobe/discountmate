groups:
- name: app
  rules:
  - alert: AppDown
    expr: probe_success == 0
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "App health probe failing"
      description: "Blackbox probe to /health on {{ $labels.instance }} is failing."